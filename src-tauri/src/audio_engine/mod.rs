pub mod devices;
pub mod plugins;
pub mod tracks;

#[derive(Default)]
pub struct AudioEngine {
    // Placeholder fields
}

impl AudioEngine {
    pub fn new() -> Self {
        Self::default()
    }
}
